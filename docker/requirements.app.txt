fastapi==0.116.1
uvicorn[standard]==0.30.6
pydantic==2.9.2
weaviate-client>=4.7,<5
# Add these so the retriever can embed queries
sentence-transformers==3.0.1
torch>=2.2.0      # CPU wheel; pip will fetch the correct one
tqdm>=4.66.0      # nice-to-have, optional
arize-phoenix